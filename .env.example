DJANGO_SECRET_KEY=REPLACE_ME_YOUR_SECRET_KEY
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=your-service.onrender.com,localhost,127.0.0.1
DJANGO_CSRF_TRUSTED_ORIGINS=https://your-service.onrender.com

# Database (Supabase)
# Set your Supabase / Postgres connection string in the Render environment
# Example: postgresql://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require
DATABASE_URL=REPLACE_ME_DATABASE_URL

# Static (optional toggles)
DJANGO_SECURE_SSL_REDIRECT=True

# Email credentials (use Render environment variables for real values)
EMAIL_USER=REPLACE_ME_EMAIL_USER
EMAIL_PASS=REPLACE_ME_EMAIL_PASS